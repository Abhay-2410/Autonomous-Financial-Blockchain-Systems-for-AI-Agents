# Moved — canonical parent host files live in:
#   infra/nitro-enclave/parent/
#
# This path is kept so older docs that linked here still resolve.
# See ../parent/vsock-proxy.yaml
allow_list:
  - { address: kms.eu-north-1.amazonaws.com, port: 443 }
